# 中文文本“去AI味”技术方案设计与实现计划

基于 `papers` 文件夹内最新研究文献（涵盖对抗性释义、AI Humanizers、StealthRL、CoPA、MASH等前沿框架），本方案旨在为 `humanize-cn` 项目设计一套高成功率、低语义损失的中文文本去AI味（对抗AI检测）技术架构。

## 核心目标
利用最新的生成式人工智能对抗技术，有效降低中文文本在专业AI检测平台（如知网AI检测、朱雀、GPTZero等）上的AI疑似度，同时保证文本的语义连贯性、学术严谨性和格式正确性。

## User Review Required
> [!IMPORTANT]  
> 方案包含多种技术路线（从免训练到强化学习微调），请您评估哪种路线最契合当前项目的计算资源和时间节点。我们是否应优先实现**免训练的提示词/对比生成方案**，还是直接投入构建基于**GRPO/RL的微调模型**？

## Open Questions
1. **目标检测平台**：目前 `humanize-cn` 主要针对哪些中文检测系统（如知网CNKI、维普、PaperPass的AI检测功能）？不同的检测器对特征的敏感度不同。
2. **算力预算**：是否有GPU资源（如A100/4090）用于本地模型的强化学习微调（RLHF/GRPO）？如果算力有限，我们将侧重于API驱动和免训练搜索攻击。

---

## 拟议技术方案 (Proposed Technical Architectures)

我们基于文献提取了4种适用于中文环境的去AI味技术路线，按实现难度和效果递进：

### 路线一：基于提示词工程与对比生成 (Contrastive Paraphrasing, CoPA)
**原理**：利用对比消除“机器特征”。(参考：CoPA)
- **实现细节**：
  1. 为大模型（如DeepSeek-V3/Qwen）构建两个系统提示：一个是“机器化（AI-style）”，一个是“高度拟人化（Human-style，注入随机性、人类惯用口语或复杂从句）”。
  2. 在生成（或改写）时，通过对比这两种提示下的词汇概率分布，人为降低机器特征高频词的采样概率。
- **优点**：零样本、免训练，直接调用现有大模型API即可实现，成本极低。
- **缺点**：对抗深度有限，可能无法绕过基于深度特征空间不变性（如DAMAGE）的最新检测器。

### 路线二：检测器引导的对抗性集束搜索 (Detector-Guided Beam Search)
**原理**：在文本生成解码的每一步，引入白盒/黑盒AI检测器作为打分器。(参考：免训练搜索对抗)
- **实现细节**：
  1. 选择一个开源的中文AI检测器（例如中文版的 Fast-DetectGPT 或基于 RoBERTa-zh 微调的二分类器）。
  2. 在本地部署大模型（如 Qwen-2.5-7B）。在自回归生成时，采用 Top-K 采样出候选词。
  3. 将包含候选词的前缀输入检测器，选择令“AI得分”最低的Token作为最终输出。
- **优点**：能够实现精确的对抗，平均可将检测率降低80%以上，且不改变模型内部权重。
- **缺点**：推理时间显著增加，因为每生成一个词都需要调用检测器打分。

### 路线三：基于文体风格的人性化微调 (Style Humanization via SFT+DPO)
**原理**：通过风格迁移学习人类的写作指纹。(参考：MASH)
- **实现细节**：
  1. **数据构建**：收集大量中文真实学术/常规语料（人类），以及对应的AI生成版本，构建成对的风格数据集。
  2. **第一阶段 (Style-SFT)**：在这些语料上对开源小模型（如 Qwen-2.5-3B）进行监督微调，让其学习人类文本中的离散文体特征（如从句嵌套、特定连词习惯、词汇爆发度）。
  3. **第二阶段 (DPO)**：将改写结果输入目标检测器。得分低的（越像人类）作为正样本，得分高的作为负样本，通过 DPO (Direct Preference Optimization) 直接优化模型。
- **优点**：推理速度快，效果稳定，适合将大模型的文本“洗”成特定学者的行文风格。

### 路线四：基于 GRPO 的强化学习对抗微调 (StealthRL / AuthorMist)
**原理**：将绕过检测器定义为强化学习的奖励最大化问题。(参考：StealthRL)
- **实现细节**：
  1. 使用 Qwen 等基础模型结合 LoRA 进行高效微调。
  2. **奖励设计 (Reward Design)**：
     - *对抗奖励*：目标检测器（如集成多个开源/闭源API）给出的“非AI”概率分数。
     - *语义奖励*：使用 BGE-m3 或 Text2Vec-chinese 计算原文本与改写文本的余弦相似度，相似度低于阈值给予严厉惩罚。
     - *流畅度约束*：计算与基线模型的 KL 散度，防止模型为了绕过检测而输出乱码（灾难性遗忘）。
  3. 采用 GRPO（群体相对策略优化）算法，利用检测器的相对反馈来更新策略，降低显存占用。
- **优点**：当前对抗成功率最高、鲁棒性最强（可达95%以上绕过率），且具备跨检测器的普遍可迁移性。
- **缺点**：工程实现复杂，需要搭建RL训练管线和代理检测环境。

---

## 阶段性实施计划 (Execution Plan)

若您批准，我建议从最快见效的 **路线一** 和 **路线二** 结合入手，搭建基础的重写引擎，然后逐步向 **路线四** 演进：

### Phase 1: 基础设施与数据基准构建
- **[NEW] `eval/benchmark.py`**: 构建一个基于已有检测器（如 Fast-DetectGPT-zh 或调用第三方API）的自动化测试框架，能够批量测试给定文本的AI疑似度。
- **[NEW] `data/hlpc_zh.json`**: 收集/构建小型的“中文人类-大模型改写数据集”（类似于HLPC），用于基准测试。
- **[NEW] `metrics/semantic.py`**: 集成中文字符级相似度和语义级相似度（如基于 `sentence-transformers`）的评估脚本。

### Phase 2: 核心改写引擎开发（轻量级）
- **[NEW] `engine/prompts.py`**: 开发用于对比生成的提示词模板（注入认知犹豫感、增加困惑度和爆发度）。
- **[NEW] `engine/copa_rewriter.py`**: 实现路线一中的对比/多轮改写逻辑，支持调用 DeepSeek 等高效 API。

### Phase 3: 对抗性强化学习框架开发（深度对抗）
*(视算力和进度而定)*
- **[NEW] `training/reward_models.py`**: 封装检测器作为 RL 的 Reward 模型。
- **[NEW] `training/grpo_trainer.py`**: 基于 `trl` 库实现针对对抗性改写的 GRPO 训练脚本。

## 验证计划 (Verification Plan)

### 自动化测试
1. 将100篇典型的AI生成中文学术文本输入重写引擎。
2. 运行基准测试，计算改写前后的 **TPR@1%FPR**（重点关注在极低假阳性率下，检测器对改写后文本的拦截率是否骤降）。
3. 统计语义相似度指标（如 BERTScore-zh > 0.90）以确保原文意图被完美保留。

### 人工验证
- 抽取样本，使用市面主流查重与AI检测工具（如知网学术不端检测系统）进行交叉验证。
- 人工阅读评估其“自然语调”和是否存在“幻觉/乱码”现象。
